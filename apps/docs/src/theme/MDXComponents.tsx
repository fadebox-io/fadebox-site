import MDXComponents from '@theme-original/MDXComponents';
import Tier from '@site/src/components/Tier';

// Registered globally so a page can mark a tier without carrying an import line.
export default {
  ...MDXComponents,
  Tier,
};
